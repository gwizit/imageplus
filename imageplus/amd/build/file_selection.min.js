define("local_imageplus/file_selection",["exports"],(function(exports){const init=config=>{const selectAllFs=document.getElementById("local-imageplus-select-all-fs");const selectAllDb=document.getElementById("local-imageplus-select-all-db");const handleSelectAllFs=e=>{e.preventDefault();const checkboxes=document.querySelectorAll("input.local-imageplus-fs-checkbox");const allChecked=Array.from(checkboxes).every((cb=>cb.checked));if(!allChecked){alert(config.warningText)}checkboxes.forEach((cb=>{cb.checked=!allChecked}));e.target.textContent=allChecked?config.selectAllText:config.deselectAllText};const handleSelectAllDb=e=>{e.preventDefault();const checkboxes=document.querySelectorAll("input.local-imageplus-db-checkbox");const allChecked=Array.from(checkboxes).every((cb=>cb.checked));if(!allChecked){alert(config.warningText)}checkboxes.forEach((cb=>{cb.checked=!allChecked}));e.target.textContent=allChecked?config.selectAllText:config.deselectAllText};if(selectAllFs){selectAllFs.addEventListener("click",handleSelectAllFs)}if(selectAllDb){selectAllDb.addEventListener("click",handleSelectAllDb)}};exports.init=init;Object.defineProperty(exports,"__esModule",{value:true})}));
